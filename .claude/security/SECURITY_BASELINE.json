{
  "version": "1.5.0",
  "date": "2026-08-31",
  "issue": 46,
  "phase": "STEP B (manifest + regression tests). STEP A (diagnostic shutdown) deliberately NOT done: ruled unsafe as drafted; B goes first.",
  "description": "Declarative inventory of the security plane. Every control names its hook, its registration (event + matchers), the files it sources, the properties that make it trustworthy, and a fixture that demonstrates it firing. A control absent from this manifest is not protected by the regression tests; a control present here cannot be unregistered or deleted silently. The gaps array names what #45 demands and does not exist yet — a manifest that lists only what exists would fake coverage.",
  "invariants": [
    "Fail-closed on every path: permission-guard v2.1.0 (#58), repo-boundary _emit_deny_on_crash trap, k8s gate blocks kubectl without an explicit --context",
    "Delegate contracts verified in source before enforcement (T16): git-safety-guard.py emits JSON on every terminal path (deny = JSON + exit 1; the JSON is authoritative)",
    "Direct registration of repo-boundary-guard.sh on Edit|Write and Agent|Task in addition to Bash: the boundary does not depend on a single wrapper"
  ],
  "controls": [
    {
      "id": "permission-pipeline",
      "kind": "hook",
      "hook": ".claude/hooks/permission-guard.sh",
      "event": "PreToolUse",
      "matchers": ["Bash", "Edit|Write", "Agent|Task"],
      "sources": [
        ".claude/hooks/git-safety-guard.py",
        ".claude/hooks/repo-boundary-guard.sh"
      ],
      "properties": [
        "v2.1.0, hardened in #58 (T16+T19): fail-closed on every path — delegate missing/crashed/empty/unparseable denies; unparseable stdin denies; premature death denies via idempotent ERR/EXIT trap (a naive trap double-echoes the deny under set -e, producing unparseable output = a bypass; the flag prevents it)",
        "Dispatches to both guards below; a real deny arriving as JSON + exit 1 is propagated intact (JSON is authoritative, not the exit code)",
        "Delegate stderr preserved: logged to ~/.ralph/permission-guard.log and re-emitted"
      ],
      "fixture": {
        "run": "printf '' | bash .claude/hooks/permission-guard.sh",
        "expect_decision": "deny",
        "expect_reason_contains": "unparseable"
      }
    },
    {
      "id": "git-safety",
      "kind": "hook",
      "hook": ".claude/hooks/git-safety-guard.py",
      "event": "PreToolUse",
      "matchers": ["Bash"],
      "sources": [],
      "properties": [
        "Blocks destructive git (reset --hard, push --force), destructive fs (rm -rf /, clean -f) and command chaining (;, &&, eval, xargs rm)",
        "Stdlib-only python (json/os/re/shlex/subprocess/sys) — no repo files sourced, no hidden dependencies",
        "Contract (verified T16): allow/ask print JSON + exit 0, deny prints JSON + exit 1 — JSON on every terminal path (CRIT-002 allow_and_exit)"
      ],
      "fixture": {
        "run": "printf '{\"tool_name\":\"Bash\",\"tool_input\":{\"command\":\"git reset --hard HEAD\"}}' | python3 .claude/hooks/git-safety-guard.py",
        "expect_decision": "deny",
        "expect_reason_contains": "BLOCKED by git-safety-guard"
      }
    },
    {
      "id": "repo-boundary",
      "kind": "hook",
      "hook": ".claude/hooks/repo-boundary-guard.sh",
      "event": "PreToolUse",
      "matchers": ["Bash", "Edit|Write", "Agent|Task"],
      "sources": [".claude/hooks/lib/worktree-utils.sh"],
      "properties": [
        "Registered DIRECTLY on Edit|Write and Agent|Task (inserted before the permission pipeline) in addition to Bash — the boundary survives a broken wrapper",
        "Own fail-closed trap _emit_deny_on_crash on ERR/EXIT (lines 15-18): a crash denies, logged to ~/.ralph/logs/repo-boundary.log",
        "Its source of lib/worktree-utils.sh (line 36) carries a '|| { inline fallback implementations }' that is NOT fail-open: it defines in-repo replacements for get_project_root/get_main_repo and the script still fails closed — it degrades with dignity",
        "Boundary semantics: the guard denies paths inside OTHER git repos under GITHUB_DIR (~/Documents/GitHub, line 25) and allows everything else BY DESIGN (is_allowed_path tail: 'Allow other paths (system, etc.)') — it is a GitHub-repo boundary, not a filesystem boundary. A path outside GITHUB_DIR is never denied, no matter what it is; fixtures must target another repo under GITHUB_DIR (the fixture isolates it by overriding HOME for the guard invocation)",
        "v2.99.0 (#61, defects found by this baseline's fixture work, all fixed with per-defect regression tests in tests/test_hooks_repo_boundary_guard.py): (1) the __CANONICALIZE_FAILED__ sentinel is now CONSULTED in is_allowed_path and denies — it used to be emitted and never compared, logging 'denying as precaution' while ALLOWING (repro: symlink loop, tested); (2) both GNU-only 'realpath -m' branches replaced by a portable deepest-existing-ancestor walk ('realpath -m' was dead on stock macOS, 'illegal option -- m'); (3) GITHUB_DIR canonicalized with the same canonicalize() used for the paths it is compared against (raw $HOME failed to match through symlinked directories)"
      ],
      "fixture": {
        "run": "d=\"$(cd \"$(mktemp -d)\" && pwd -P)\" && mkdir -p \"$d/Documents/GitHub/fr\" && git -C \"$d/Documents/GitHub/fr\" init -q && printf '{\"tool_name\":\"Edit\",\"tool_input\":{\"file_path\":\"%s/Documents/GitHub/fr/x.md\"}}' \"$d\" | HOME=\"$d\" bash .claude/hooks/repo-boundary-guard.sh; rc=$?; rm -rf \"$d\"; exit $rc",
        "expect_decision": "deny",
        "expect_reason_contains": "REPO BOUNDARY"
      }
    },
    {
      "id": "k8s-context",
      "kind": "hook",
      "hook": ".claude/hooks/k8s-context-guard-v2.py",
      "event": "PreToolUse",
      "matchers": ["Bash"],
      "sources": [
        ".claude/hooks/k8s_context_guard/cloud_operation_gate.py",
        ".claude/hooks/k8s_context_guard/context_classification.py",
        ".claude/hooks/k8s_context_guard/minikube_context.py",
        ".claude/hooks/k8s_context_guard/script_operation_inspector.py"
      ],
      "properties": [
        "Context-aware: every kubectl/helm/minikube command must declare --context explicitly (kubectl_context_required otherwise); --context must be a static name (kubectl_context_not_static)",
        "Factual minikube verification rather than name-matching; kubectl verbs delegated from git-safety by design (handoff)",
        "Known over-reach, cataloged (QTEAM_FAILURE_MODES #22): can block unrelated commands that merely reference hook scripts — visible, not silent"
      ],
      "fixture": {
        "run": "printf '{\"tool_name\":\"Bash\",\"tool_input\":{\"command\":\"kubectl get pods\"}}' | python3 .claude/hooks/k8s-context-guard-v2.py",
        "expect_decision": "deny",
        "expect_reason_contains": "kubectl_context_required"
      }
    },
    {
      "id": "worktree-utils",
      "kind": "lib",
      "hook": ".claude/hooks/lib/worktree-utils.sh",
      "event": null,
      "matchers": [],
      "sources": [],
      "sourced_by": [".claude/hooks/repo-boundary-guard.sh"],
      "properties": [
        "Provides get_project_root / get_main_repo (worktree-aware, via --git-common-dir) to the boundary guard and the worktree guards",
        "Not registered itself: consumed via source. repo-boundary-guard.sh carries inline fallbacks if this lib is unreachable (see repo-boundary properties)"
      ],
      "fixture": {
        "run": "bash -c 'source .claude/hooks/lib/worktree-utils.sh && get_project_root'",
        "expect_kind": "bash_function",
        "expect_stdout_contains": "multi-agent-ralph-loop"
      }
    },
    {
      "id": "skill-security",
      "kind": "hook",
      "hook": ".claude/hooks/skill-validator.sh",
      "event": "PreToolUse",
      "matchers": ["Skill"],
      "sources": [".claude/hooks/skill-validator.allowlist"],
      "properties": [
        "T48 (#68): six deterministic patterns over the ONE SKILL.md about to load (invisible unicode, base64+exec, exfil IOCs, pipe-to-shell, secret literals, quote-aware override); context-awareness is a REQUIREMENT - the macos-cleaner extracts (real third-party file matching attack patterns while teaching the defense) are the regression fixture",
        "Auditable allowlist: an entry without a written reason is invalid (ignored, skill stays blocked - fail-closed); an exempted pattern matching nothing is reported STALE so the list cannot rot",
        "T44 (#67): the H70 skill.yaml domain is preserved - present-but-invalid skill.yaml denies; nameless and out-of-domain names allow silently",
        "T62 (#72): FAILS OPEN BY DESIGN, deliberately - a validator bug must not take down the skill system (permission-guard covers unparseable payloads on the same matcher); empty/unparseable stdin emits allow with a clean exit 0, never by crashing (the accidental rc-1 allow is fixed)"
      ],
      "fixture": {
        "run": "D=$(mktemp -d) && mkdir -p \"$D/.claude/skills/evil\" && printf '# evil\\n\\342\\200\\213zwsp payload\\n' > \"$D/.claude/skills/evil/SKILL.md\" && printf '{\"tool_input\":{\"skill\":\"evil\"}}' | HOME=\"$D\" bash .claude/hooks/skill-validator.sh; R=$?; rm -rf \"$D\"; exit $R",
        "expect_decision": "deny",
        "expect_reason_contains": "unicode_invisible"
      }
    }
  ],
  "gaps": [
    {
      "id": "secrets-ordinary-work",
      "from_issue": 45,
      "status": "control-declarado",
      "description": "#45 demands preventing secrets from entering ordinary (non-audit) work. RESOLVED at the mechanism level (PR3-C1, 2026-08-31): a deterministic write-time gate now exists — .claude/hooks/secrets-write-guard.py (PreToolUse over Write/Edit/MultiEdit) denies 6 closed secret patterns (PEM headers, sk- keys, GitHub tokens, AKIA ids, URL-embedded credentials, credential assignment literals) with an actionable message and a versioned path allowlist; fail-closed on unparseable input per the security-plane invariant. 21-assertion matrix in tests/security/test-secrets-write-guard.sh (registered in the unit gate; includes pure-prose allows and the lead's Write+new_string repro); three-result proof recorded (pattern-poisoning fails the suite). ACTIVATION IS USER-SIDE: registering it in ~/.claude/settings.json (matcher Edit|Write) is lead+user, as with every settings change. The old audit-secrets.js PostToolUse audit remains deregistered (see 'deregistered') as /ship evidence only.",
      "control": {
        "kind": "hook",
        "hook": ".claude/hooks/secrets-write-guard.py",
        "event": "PreToolUse",
        "matchers": ["Edit|Write|MultiEdit"],
        "fixtures": {
          "positive": "printf '%s' '{\"tool_name\":\"Write\",\"tool_input\":{\"file_path\":\"/tmp/a.py\",\"content\":\"aws_id = \\\"AKIAABCDEFGHIJKLMNOP\\\"\"}}' | python3 .claude/hooks/secrets-write-guard.py  -> deny, exit 1",
          "negative": "Write of normal code (type hints, env lookups) -> allow, exit 0",
          "allowlist": "same PEM content written to a path matching **/fixtures/** -> allow with allowlist reason"
        },
        "properties": [
          "stdlib-only, closed patterns with fixed anchors/charsets/minimum lengths — no vague regexes",
          "fail-closed on unparseable stdin, missing file_path, or payloads with NO known content field; present-and-legible content NEVER denies by shape (pure-prose regression found by the user, fixed in PR3-C1-r2: every known content field is scanned wherever it appears)",
          "never echoes the matched secret — reason names the pattern and location only",
          "latency measured with the hotpath-probe method (N=200, median/p90/max in results/pr3-c1-latency.txt)"
        ],
        "suite": "tests/security/test-secrets-write-guard.sh (gate-registered)"
      }
    },
    {
      "id": "red-toxic",
      "from_issue": 45,
      "status": "no-hook",
      "description": "No control screens for RED/toxic content in what the agent produces or relays. Nothing exists to register."
    },
    {
      "id": "mcp-egress",
      "from_issue": 45,
      "status": "control-declarado",
      "description": "MCP tool calls bypass the Bash/Edit permission surface. RESOLVED via native surface (PR3-C3a, 2026-08-31): the active harness (Claude Code 2.1.251) exposes MCP calls to PreToolUse with the full mcp__<server>__<tool> name and tool_input arguments, and permissions rules support the mcp__<server>__<tool> syntax (live in the user's active settings deny list). No guard code was needed — the mechanism already existed; what was missing was the proof.",
      "control": {
        "kind": "native-permission-surface",
        "mechanism": "permissions deny/ask rules with mcp__<server>__<tool> syntax, plus PreToolUse hooks matching mcp__.* (both proven by experiment PR3-C3a)",
        "proven_by": "scripts/experiments/mcp-surface/ (runner + README; full report in docs/security/MCP_SURFACE_EXPERIMENT_PR3-C3a.md)",
        "reproduce": "bash scripts/experiments/mcp-surface/run-experiment.sh A (Bash control AND the MCP call must both appear in the hook log) && bash scripts/experiments/mcp-surface/run-experiment.sh B (matcher mcp__.*: ONLY the MCP call appears)",
        "policy_note": "The list of denied servers/tools is POLICY, delegated to the user's active settings (permissions.deny) — this manifest declares the mechanism, not the server list."
      }
    },
    {
      "id": "package-manager",
      "from_issue": 45,
      "status": "no-hook",
      "description": "brew/pip/npm installs are governed by a RULE (global CLAUDE.md approval requirement) but by no hook — rules constrain the model, not the tool call. git-safety does not cover package managers."
    },
    {
      "id": "symlink-escape",
      "from_issue": 45,
      "status": "resolved",
      "description": "repo-boundary-guard.sh checks paths, but symlink-following escapes (an in-repo symlink pointing outside) are only partially covered. Declared partial until a fixture proves symlink resolution is boundary-checked.",
      "resolution": "PR3-C5 fixture-first: the fixture measured 4/4 escapes silently ALLOWED (bash mention, Write file_path, depth-2 symlink chain, symlink->/tmp) because canonicalization resolved the symlink BEFORE the boundary comparisons, so the resolved path fell through 'Allow other paths'. Fixed in is_allowed_path: a path whose SUPERFICIAL token claims a trusted boundary (current repo, working tree, GITHUB_DIR) but whose canonical location does not is denied — no exceptions, not even /tmp. Never-claimed paths, same-repo symlinks and pure readonly commands remain usable; a redirect makes the whole command a write-scan, so a read through an escaping symlink in a redirecting command is also denied (pinned as designed). Evidence: tests/security/fixtures/symlink_escape_runner.py (pre-fix 4 FAIL, post-fix 9/9 PASS), registered as gate suite 47.",
      "resolved_by": "worktree-zc-4 PR3-C5"
    }
  ],
  "deregistered": [
    {
      "id": "audit-secrets-hotpath",
      "hook": ".claude/hooks/audit-secrets.js",
      "was_registered": "PostToolUse(*) in settings.json.example and the ACTIVE user settings",
      "destination": "retained in the repo as explicit /ship audit evidence; registered nowhere",
      "reason": "#69 §1B: PostToolUse is audit-only (its own header: cannot block/redact/modify); detection-after-the-fact is not the demanded control, so it is not an enforcing security survivor. Removed from settings.json.example by PR3-C7 (PR 3 slice 1, order 7->6->5->4->1->3->2). Removal from the ACTIVE user settings is lead+user side. Companion gap secrets-ordinary-work stays OPEN until a deterministic PreToolUse write gate exists.",
      "date": "2026-08-31",
      "refs": ["#69", "#45"]
    },
    {
      "id": "promptify-security-registration",
      "hook": ".claude/hooks/promptify-security.sh",
      "was_registered": "PreToolUse(Agent|Task) in the ACTIVE user settings only (never in settings.json.example nor the security-only profile)",
      "destination": "retained as an unregistered library; exercised by tests/promptify-integration; no runtime consumer in the repo",
      "reason": "As a hook it read no stdin and had no top-level dispatch, so it emitted no permissionDecision (implicit allow): the appearance of a security control with zero enforcement. PR3-C7 removes nothing here (there was nothing repo-side to remove) and names it so it cannot return silently; if adopted, register explicitly AND move it out of this array in the same PR. Removal from the ACTIVE user settings is lead+user side.",
      "date": "2026-08-31",
      "refs": ["#69", "#45"]
    }
  ]
}
