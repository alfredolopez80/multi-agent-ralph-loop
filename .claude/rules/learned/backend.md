---
paths:
  - "src/services/**/*"
  - "src/api/**/*"
  - "src/controllers/**/*"
  - "**/*.ts"
---

# Backend Rules (Auto-learned)

Rules from procedural memory. Confidence >= 0.7, usage >= 3.

## Rules

- Uses async/await for asynchronous operations
- Implements metrics/observability
- Uses async/await for asynchronous operations. Implements error handling. Implements structured logging
- Implements caching strategy. Uses schema validation

---

*Generated: 2026-02-15 22:58. Source: procedural memory (4 rules)*
